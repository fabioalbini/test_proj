{
  "family": "capistrano-deploy-staging",
  "requiresCompatibilities": [
    "FARGATE"
  ],
  "networkMode": "awsvpc",
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "ecsTaskExecutionRole",
  "containerDefinitions": [
    {
      "name": "cap-deploy",
      "essential": true,
      "command": [
        "bundle", "exec", "cap", "staging", "deploy"
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "capistrano-deploy-staging",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "deploy-staging"
        }
      },
      "secrets": [
        {
          "name": "STAGING_SECRETS",
          "valueFrom": "arn:aws:secretsmanager:us-east-1:442014623269:secret:deploy/staging-NfAu1u"
        }
      ]
    }
  ]
}
